# Core point-cloud and raster processing dependencies.
# The Electron app expects these to be installed in ./venv.

laspy[lazrs]>=2.6,<3
matplotlib>=3.10,<4
numpy>=2,<3
rasterio>=1.4,<2
scipy>=1.14,<2
